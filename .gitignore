# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# AMPS runtime state: SOW files, transaction log, admin stats. Created by
# server/scripts/amps.sh, deleted by `amps.sh reset`. Never committed -- the
# whole point is that it is the instance's own state.
server/data/

# Client-side bookmark and publish stores.
build/client-state/

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.classpath
.project

# OS
.DS_Store
