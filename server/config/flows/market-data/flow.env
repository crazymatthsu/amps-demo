# Layer 3, business flow "market-data": the 500 GB/day worked case
# (docs/src/high-volume-market-data.md). Nothing host-level needs overriding
# here either -- the sizing choices this flow demonstrates
# (MinJournalSize, PreallocatedJournalFiles, the retention Action) are all
# inside its amps-config.xml.
