# Layer 3, business flow "bounded-retention": the minimum-persistent-footprint
# variant (docs/src/transaction-log-sizing.md). Nothing host-level needs
# overriding -- the aggressive journal rollover this flow demonstrates is
# entirely inside its amps-config.xml, not something an env file can reach.
