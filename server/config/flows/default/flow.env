# Layer 3, business flow "default": the baseline demo instance (dynamic
# topics, SOW, delta, FIX/NVFIX, journal lab -- see docs/src/runbook.md).
# Nothing here needs to differ by flow; this file exists so every flow
# folder has the same three files and the layering order stays uniform.
