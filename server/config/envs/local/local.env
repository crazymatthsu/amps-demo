# Layer 2, environment "local": a laptop running podman, one instance at a
# time. The repo defaults already assume this shape, so there is nothing to
# override -- this file exists so the four environments are symmetric, and so
# a real override has somewhere to go without inventing new plumbing.
