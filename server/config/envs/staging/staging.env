# Layer 2, environment "staging": a dedicated bare-metal or VM box, one
# instance at a time, so the default ports are fine. Unlike a laptop it is
# already the right architecture -- no emulation needed.
AMPS_TZ=UTC
AMPS_PLATFORM=
